# דיווח שעות (Reporting-hours)

אפליקציית אנדרואיד לדיווח עצמי של מרצים על שעות עבודה לפי מקום עבודה:
שעות אקדמיות, הפסקות, שכר, נסיעות וסיכומים חודשיים.

החזון המלא ותוכנית העבודה: [WISHLIST.md](./WISHLIST.md).

## סטאק

- **React Native + Expo (SDK 57)** ב־TypeScript — קוד אחד שרץ גם כ**אתר** (react-native-web)
  וגם כ**אפליקציית אנדרואיד**.
- **expo-router** לניווט מבוסס־קבצים (תיקיית `app/`).
- כיווניות **RTL** נכפית דרך [lib/rtl.ts](./lib/rtl.ts) ובתבנית ה־HTML [app/+html.tsx](./app/+html.tsx).
- אחסון מקומי בלבד בשלב זה (ללא שרת) דרך `@react-native-async-storage/async-storage`,
  שממופה ל־`localStorage` ב־web. עטיפת JSON: [lib/storage.ts](./lib/storage.ts).

## אתר (web)

- בנייה מקומית: `npm run build:web` → פלט סטטי בתיקיית `dist/`.
- פריסה: workflow ב־[.github/workflows/deploy-web.yml](./.github/workflows/deploy-web.yml)
  בונה ופורס ל־**GitHub Pages** בכל push ל־`main`.
- כתובת: `https://yanivmalka.github.io/Reporting-hours/`
  (דורש הפעלה חד־פעמית: Settings → Pages → Source = "GitHub Actions").
- ה־`baseUrl` מוגדר ל־`/Reporting-hours` ב־`app.json` בשל נתיב ה־project-site של Pages;
  יש לשנותו אם עוברים לדומיין ייעודי.

## APK (אנדרואיד)

תשתית ה־build ל־APK (EAS) תתווסף בשלב המעבר לאריזה (שלב 8). עד אז מריצים על אנדרואיד
דרך Expo Go (`npm start` + סריקת QR).

## מבנה

| נתיב | תיאור |
|------|-------|
| `app/_layout.tsx` | Stack navigator, ערכת צבעים, כפיית RTL |
| `app/+html.tsx` | תבנית HTML לגרסת web (עברית, RTL) |
| `app/index.tsx` | מסך בית עם ניווט למסכים |
| `app/report.tsx` | טופס דיווח יום עבודה: מקום עבודה, תאריך, טווח שעות, הפסקות, זמן נטו ושעות אקדמיות (שלבים 2–3) |
| `app/reports.tsx` | רשימת הדיווחים שנשמרו, מהחדש לישן (שלב 2) |
| `app/workplaces.tsx` | רשימת מקומות העבודה + כפתור הוספה (שלב 1) |
| `app/workplace-form.tsx` | טופס הוספה / עריכה / מחיקה של מקום עבודה (שלב 1) |
| `app/settings.tsx` | מונה "דקות עבודה מצטברות" ופדיון לשעות אקדמיות (שלב 3); שאר התגיות — שלב 6 |
| `lib/storage.ts` | עטיפת JSON מעל AsyncStorage (מקומי, עובד גם ב־web) |
| `lib/workplaces.ts` | מודל `Workplace` ופעולות טעינה/שמירה/עדכון/מחיקה |
| `lib/reports.ts` | מודל `WorkReport`, פעולות שמירה/מחיקה, חישוב זמן נטו ופירוק לשעות אקדמיות |
| `lib/academic.ts` | המרת זמן נטו לשעות אקדמיות (45 דק׳) ודקות עודפות (שלב 3) |
| `lib/accumulated.ts` | מונה "דקות עבודה מצטברות" — צבירה, פדיון ואיפוס, אחסון מקומי (שלב 3) |
| `lib/travel.ts` | חישוב עלות נסיעות לכל מקום עבודה: עלות לכיוון × נסיעות ליום × ימי עבודה שדווחו (שלב 5) |
| `lib/time.ts` | עזרי זמן ותאריך: פענוח `HH:MM`, אימות תאריך, ותצוגת משך בעברית |
| `components/` | רכיבים משותפים (`Screen`, `NavCard`, `Placeholder`, `Button`, `FormField`, `TextField`, `SegmentedControl`, `WorkplaceCard`, `ReportCard`, `Toast`) |
| `theme/colors.ts` | צבעים, ריווח ורדיוסים |

## הרצה

```bash
npm install
npm start          # מפעיל את Metro; לחיצה על a פותחת אמולטור אנדרואיד / Expo Go
npm run android    # הרצה ישירה על אנדרואיד
npm run typecheck  # בדיקת טייפים
npx expo-doctor    # בדיקת תקינות הפרויקט
```

## סטטוס

- [x] **שלב 0 — יסודות טכניים:** שלד Expo רץ (web + אנדרואיד), ניווט, RTL, ערכת צבעים,
      מסך בית, פריסת אתר ל־GitHub Pages.
- [x] **שלב 1 — פרופיל ומקומות עבודה:** מודל `Workplace`, אחסון מקומי, מסך רשימה וטופס
      הוספה / עריכה / מחיקה. הנתונים נטענים בהפעלה ונשמרים אוטומטית.
- [x] **שלב 2 — מסך דיווח בסיסי:** מודל `WorkReport`, טופס דיווח (מקום עבודה, תאריך,
      טווח שעות, כמה הפסקות) עם חישוב זמן עבודה נטו חי, ורשימת דיווחים נשמרת מקומית.
- [x] **שלב 3 — שעות אקדמיות ודקות מצטברות:** המרת זמן נטו לשעות אקדמיות (45 דק׳)
      בטופס ובכל דיווח; הדקות העודפות נכנסות למונה "דקות עבודה מצטברות" (בחירה בין
      "למונה המצטבר" ל"לכלול בדיווח"), עם הודעה קופצת בכל הוספה; מסך ההגדרות מציג את
      המונה ומאפשר פדיון של שעות אקדמיות שלמות ואיפוס.
- [ ] שלב 4 — חישוב שכר
- [x] **שלב 5 — חישוב נסיעות:** מודול `lib/travel.ts` מחשב עלות נסיעות מצטברת לכל מקום
      עבודה (תחבורה ציבורית בלבד), לפי ימי העבודה הייחודיים שדווחו. הסכום מוצג בכרטיס
      מקום העבודה במסך "מקומות עבודה".
- [ ] שלב 6 — הגדרות משתמש: תגיות ודשבורד חודשי
- [ ] שלב 7 — סיכום חודשי אוטומטי והתראות
- [ ] שלב 8 — ליטוש ואריזה
